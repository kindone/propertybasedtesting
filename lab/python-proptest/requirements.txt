# Install:  python -m venv .venv && source .venv/bin/activate  (Windows: .venv\Scripts\activate)
#           pip install -r requirements.txt
# Run:      python src/poc_basic.py
#
# Pin a revision by changing the fragment after @ (branch, tag, or commit).
# PyPI releases: https://pypi.org/project/python-proptest/ — use "pip install python-proptest==..." instead if you prefer.

python-proptest @ git+https://github.com/kindone/python-proptest.git@main
